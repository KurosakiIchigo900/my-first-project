server:
  servlet:
    context-path: /v2
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aravind
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
